name: scrcpy_app
description: Scrcpy desktop client — Android screen mirroring.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  path: ^1.9.0
  autoglm_logger:
    path: ../packages/autoglm_logger
  scrcpy_view:
    path: ../scrcpy_view
  scrcpy_adapters:
    path: ../packages/scrcpy_adapters
  flutter_inappwebview: ^6.1.5
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
